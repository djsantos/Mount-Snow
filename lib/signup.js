/*
module lib/signup


module.exports = {
  signup:CreateAccount
};
// imports the user library
var Userlib = require('../lib/user')

/*
Creates the account and adds the new user to the user data base


function CreateAccount(name, email, password, confirmPass, username, cb){
	if(name && email && password && confirmPass && (password === confirmPass) && username){
		Userlib.createUser(name,email,password,username);
		console.log(name);
		cb(undefined);
		//return true;
	}
	else{
		console.log(password);
		cb('error');
		//return false;
	}
};*/
